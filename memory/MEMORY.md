# Memory index

One line per memory file. Format: `- [name](file.md) — hook`. Read fully at Phase 0 of every task.

*(empty — `/init-project` writes the first entry when a project is created from this scaffold)*
